# Historia #11: Ver Dashboard (Empleado)

**Estado:** Finalizada ✅
**Fecha Análisis Arquitectónico:** 2025-11-23
**Fecha Refinamiento:** 2025-11-23
**Fecha Implementación:** 2026-02-24
**Fecha Pruebas Manuales:** 2026-02-24
**Origen:** Importada de HISTORIAS_DE_USUARIO.md
**Estimación:** 3 Puntos

---

**Como:** Empleado (Operario)
**Quiero:** Ver un panel de control (Dashboard) al iniciar sesión.
**Para:** Tener una vista general del estado del negocio.

**Criterios de Aceptación:**
*   El dashboard muestra KPIs actualizados: Maquinaria Disponible, Contratos Activos y Facturas Pendientes.
*   Los datos se actualizan en tiempo real o al cargar la página.

---

## Análisis Arquitectónico (Arquitecto)

### Decisiones de Diseño

**Patrón Arquitectónico:** CQRS (Query Side) - Agregación de Datos

**Justificación:**
*   **Eficiencia:** Permite realizar consultas optimizadas (`COUNT`) sin cargar entidades completas.
*   **Desacoplamiento:** La capa de aplicación orquesta la obtención de datos de múltiples dominios (Maquinaria, Alquiler, Facturación) sin acoplarlos entre sí.
*   **Escalabilidad:** Facilita la adición de nuevos KPIs en el futuro sin modificar los dominios base.

**Componentes Principales:**

*   **Aplicación (Consulta):**
    *   `ConsultaDashboardOperario`: DTO de solicitud (vacío o con filtros).
    *   `ManejadorDashboardOperario`: Orquestador que invoca los DAOs.
    *   `DashboardOperarioDTO`: Objeto de transferencia con los KPIs.

*   **Dominio (Puertos):**
    *   `MaquinariaDao`: Añadir `long contarPorEstado(EstadoMaquinaria)`.
    *   `AlquilerDao`: Añadir `long contarPorEstado(EstadoAlquiler)`.
    *   `FacturaDao`: Añadir `long contarPorEstado(EstadoPago)`.

*   **Infraestructura:**
    *   `ConsultaControladorDashboard`: Endpoint REST.
    *   Repositorios JPA implementando los conteos (`countBy...`).

### Especificaciones Técnicas

**API Endpoint:**
*   `GET /api/maquimu/v1/dashboard/operario`
*   **Respuesta:**
    ```json
    {
      "maquinariaDisponible": 15,
      "alquileresActivos": 8,
      "facturasPendientes": 3
    }
    ```

**Integraciones:**
*   El manejador agrega datos de 3 fuentes distintas.
*   Consultas de solo lectura optimizadas.

### Validación Arquitectónica
**Validado por:** Usuario (Luis)
**Fecha de validación:** 2025-11-23
**Feedback incorporado:**
*   ✅ Aprobado patrón de agregación en capa de aplicación.
*   ✅ KPIs definidos: Maquinaria Disponible, Alquileres Activos, Facturas Pendientes.

---

## Contexto Técnico (SM)

### Base del Refinamiento
**Análisis arquitectónico utilizado:** SÍ
**Decisiones adoptadas:** CQRS Agregación, Consultas COUNT optimizadas.

### Análisis de Impacto
*   **Backend:**
    *   No requiere nuevas tablas.
    *   Requiere modificar 3 DAOs existentes (`Maquinaria`, `Alquiler`, `Factura`).
    *   Nuevo controlador de Dashboard.
*   **Frontend:**
    *   Nuevo componente en `AdminModule`.
    *   Integración con API de dashboard.

### Componentes Técnicos a Crear/Modificar

**1. Módulo Dominio (Modificaciones):**
*   `MaquinariaDao`: Añadir `long contarPorEstado(EstadoMaquinaria)`.
*   `AlquilerDao`: Añadir `long contarPorEstado(EstadoAlquiler)`.
*   `FacturaDao`: Añadir `long contarPorEstado(EstadoPago)`.

**2. Módulo Aplicación (Nuevo):**
*   `ConsultaDashboardOperario` (DTO).
*   `ManejadorDashboardOperario`:
    *   Inyecta los 3 DAOs.
    *   Llama a `contarPorEstado` en paralelo o secuencial.
    *   Construye `DashboardOperarioDTO`.

**3. Módulo Infraestructura:**
*   **Repositorios JPA:** Implementar `countByEstado` (Spring Data lo genera automático).
*   **Adaptadores DAO:** Exponer el conteo.
*   **Controlador:** `ConsultaControladorDashboard` -> `GET /api/maquimu/v1/dashboard/operario`.

**4. Frontend:**
*   `EmployeeDashboardComponent` (Web).

---

## Tareas de Implementación (SM)

### Fase 1: Backend - Persistencia (Conteos)
- [x] **Actualizar Puertos y Adaptadores** (AC: 1)
    - [x] `MaquinariaDao` y `JpaMaquinariaRepository`: Ya existia conteo por estado.
    - [x] `AlquilerDao` y `JpaAlquilerRepository`: Añadido `contarPorEstado` + `countByEstado`.
    - [x] `FacturaDao` y `JpaFacturaRepository`: Ya existia `contarPorEstadoPago`.

### Fase 2: Backend - Capa de Aplicación (Agregación)
- [x] **Implementar Consulta Dashboard** (AC: 1)
    - [x] Crear `DashboardOperarioDTO` (campos: maquinariaDisponible, alquileresActivos, facturasPendientes).
    - [x] Crear `ManejadorDashboardOperario`:
        - [x] Invocar `maquinariaDao.contarPorEstado(DISPONIBLE)`.
        - [x] Invocar `alquilerDao.contarPorEstado(ACTIVO)`.
        - [x] Invocar `facturaDao.contarPorEstadoPago(PENDIENTE)`.
        - [x] Retornar DTO.

### Fase 3: Backend - API REST
- [x] **Endpoint Dashboard** (AC: 2)
    - [x] Crear `ConsultaControladorDashboard`.
    - [x] Endpoint `GET /api/maquimu/v1/dashboard/operario`.

### Fase 4: Frontend Web (Angular)
- [x] **Servicio Dashboard** (AC: 2)
    - [x] Crear `DashboardService` en `core/services`.
    - [x] Método `getOperarioStats()`.
- [x] **Vista Dashboard** (AC: 1)
    - [x] Actualizar `DashboardComponent` existente con datos reales.
    - [x] 3 tarjetas KPI dinámicas + spinner + error handling + accesos rápidos.

### Fase 5: Testing
- [x] **Test de Integración**
    - [x] Verificar que los conteos coinciden con los datos en BD.

---

## Estimación (SM)

**Puntos de Historia:** 3

**Justificación:**
*   **Complejidad Baja-Media:** Lógica de agregación simple.
*   **Esfuerzo:** Modificar 3 repositorios y crear una vista.
*   **Riesgo Bajo:** Consultas estándar.
