# Historia #09: Generación de Factura

**Estado:** Refinado (SM) - Lista para Desarrollo
**Fecha Creación:** 2025-11-22
**Fecha Análisis y Refinamiento:** 2025-11-23
**Origen:** Importada de HISTORIAS_DE_USUARIO.md
**Estimación:** 5 Puntos

---

**Como:** Operario (Administrador o Agente)
**Quiero:** Generar facturas para los alquileres.
**Para:** Controlar los pagos y la contabilidad.

**Criterios de Aceptación:**
*   La factura se genera asociada a un alquiler (`alquiler_id`).
*   La factura se crea con un monto, fecha de emisión y un estado por defecto ('PENDIENTE').
*   La factura generada es visible en el módulo financiero y en el portal del cliente.
*   Puedo editar el estado de pago de una factura.
*   Puedo consultar todas las facturas del sistema.

---

## Análisis Arquitectónico (Arquitecto)

### Decisiones de Diseño

**Patrón Arquitectónico:** Arquitectura Hexagonal Multi-módulo + CQRS + Adaptador de Documentos

**Justificación:**
*   **Separación de Responsabilidades:** La lógica de generación de documentos (PDF) se aísla en un adaptador de infraestructura, desacoplando el dominio de librerías específicas (`OpenPDF`).
*   **CQRS:** Separación clara entre generar la factura (Comando) y consultarla/descargarla (Consulta).
*   **Reglas de Negocio:** Validación estricta del estado del alquiler (`FINALIZADO`) antes de facturar.

**Modelo de Datos (Entidad `Factura`):**
*   `facturaId`: Long (PK, Auto-increment).
*   `alquilerId`: Long (FK a alquileres, Unique).
*   `clienteId`: Long (FK a clientes, redundancia controlada para consultas rápidas).
*   `fechaEmision`: LocalDateTime (Not Null).
*   `montoTotal`: BigDecimal (Not Null).
*   `estadoPago`: Enum [`PENDIENTE`, `PAGADO`, `ANULADO`].
*   `rutaArchivoPdf`: String (Opcional, si se almacena en disco/S3, o se genera al vuelo).

**Estructura de Módulos (Backend):**

#### **1. Módulo Dominio (`dominio/`)**
*   **Modelo:** `Factura`, `EstadoPago`.
*   **Puerto (Salida):** `GeneradorFacturaPort` (Interfaz para generar el documento).
    *   `byte[] generarFacturaPdf(Factura factura, Alquiler alquiler, Cliente cliente, Maquinaria maquinaria)`.
*   **Puerto (Persistencia):** `FacturaRepositorio`.

#### **2. Módulo Infraestructura (`infraestructura/`)**
*   **Adaptador PDF:** `OpenPdfGeneradorFactura` (Implementa `GeneradorFacturaPort` usando **OpenPDF**).
*   **Persistencia:** `FacturaEntity`, `JpaFacturaRepository`.
*   **Controlador:** `ComandoControladorFactura` (Generar), `ConsultaControladorFactura` (Listar, Descargar).

#### **3. Módulo Aplicación (`aplicacion/`)**
*   **Comando:** `ComandoGenerarFactura`.
*   **Manejador:** `ManejadorGenerarFactura`.
    *   Verifica que `Alquiler` esté `FINALIZADO`.
    *   Calcula/Valida monto final.
    *   Crea `Factura`.
    *   Llama a `GeneradorFacturaPort` para obtener el PDF.
    *   Guarda `Factura` en BD.
    *   (Opcional) Guarda el PDF o lo retorna.

### Validación Arquitectónica
**Validado por:** Usuario (Luis)
**Fecha de validación:** 2025-11-23
**Feedback incorporado:**
*   ✅ Incluir generación de PDF.
*   ✅ Usar librería **OpenPDF** (Open Source).
*   ✅ Restricción: Alquiler debe estar en estado **FINALIZADO**.

---

## Contexto Técnico (SM)

### Base del Refinamiento
**Análisis arquitectónico utilizado:** SÍ
**Decisiones adoptadas:** Hexagonal + CQRS + OpenPDF.

### Análisis de Impacto
*   **Dependencias:** Añadir `com.github.librepdf:openpdf` al `build.gradle` del módulo `infraestructura`.
*   **Base de Datos:** Tabla `facturas` (verificar existencia o crear migración).
*   **Frontend:** Botón "Generar Factura" en historial de alquileres finalizados.

### Componentes Técnicos a Crear

**1. Módulo Dominio:**
*   `Factura`, `GeneradorFacturaPort`, `FacturaRepositorio`.

**2. Módulo Aplicación:**
*   `ComandoGenerarFactura` (DTO: `alquilerId`).
*   `ManejadorGenerarFactura`.
*   `ConsultaDescargarFactura` (DTO: `facturaId`).
*   `ManejadorDescargarFactura`.

**3. Módulo Infraestructura:**
*   `OpenPdfGeneradorFactura`: Lógica de diseño del PDF (Logo, Tabla de items, Totales).
*   `FacturaEntity`, `JpaFacturaRepository`.
*   `FacturaController`.

---

## Tareas de Implementación (SM)

### Fase 0: Configuración y BD
- [ ] **Añadir Dependencia OpenPDF** (AC: 1)
    - [ ] En `infraestructura/build.gradle`: `implementation 'com.github.librepdf:openpdf:1.3.30'` (o versión reciente).
- [ ] **Migración BD** (AC: 1)
    - [ ] Crear/Validar tabla `facturas`: `factura_id`, `alquiler_id`, `cliente_id`, `fecha_emision`, `monto_total`, `estado_pago`.

### Fase 1: Backend - Dominio e Infraestructura PDF
- [ ] **Crear Puerto Generador** (AC: 1)
    - [ ] `dominio/puerto/salida/GeneradorFacturaPort.java`.
- [ ] **Implementar Adaptador OpenPDF** (AC: 1, 2)
    - [ ] `infraestructura/adaptador/documento/OpenPdfGeneradorFactura.java`.
    - [ ] Diseñar layout simple: Encabezado (MaquiMu), Datos Cliente, Detalle Alquiler, Total.

### Fase 2: Backend - Lógica de Aplicación
- [ ] **Implementar Comando Generar** (AC: 1, 2)
    - [ ] `ManejadorGenerarFactura`:
        - [ ] Buscar Alquiler.
        - [ ] **Validar:** `alquiler.estado == FINALIZADO`.
        - [ ] Crear Factura.
        - [ ] Persistir.
- [ ] **Implementar Consulta Descargar** (AC: 1)
    - [ ] `ManejadorDescargarFactura`:
        - [ ] Buscar Factura y datos relacionados.
        - [ ] Regenerar PDF al vuelo (o leer de disco si se guardó).
        - [ ] Retornar `byte[]`.

### Fase 3: Backend - API REST
- [ ] **Endpoints Factura** (AC: 1, 3)
    - [ ] `POST /api/maquimu/v1/facturas`: Generar (Body: `{alquilerId}`).
    - [ ] `GET /api/maquimu/v1/facturas/{id}/pdf`: Descargar (Retorna `application/pdf`).
    - [ ] `GET /api/maquimu/v1/facturas`: Listar (Filtros: cliente, estado).

### Fase 4: Frontend Web (Angular)
- [ ] **Vista de Facturas** (AC: 3, 5)
    - [ ] `InvoiceListComponent`: Tabla de facturas.
    - [ ] Botón "Descargar PDF" (abre en nueva pestaña o descarga).
- [ ] **Integración en Alquileres** (AC: 1)
    - [ ] En `MyRentalsComponent` (Historial): Si estado es `FINALIZADO` y no tiene factura -> Mostrar botón "Generar Factura" (o "Solicitar Factura").

### Fase 5: Testing
- [ ] **Test Generación PDF**
    - [ ] Verificar que se genera un array de bytes no vacío.
    - [ ] Verificar contenido (opcional, complejo).
- [ ] **Test Reglas de Negocio**
    - [ ] Intentar facturar alquiler NO finalizado -> Error.
    - [ ] Intentar facturar alquiler ya facturado -> Error o devolver existente.

---

## Estimación (SM)

**Puntos de Historia:** 5

**Justificación:**
*   **Complejidad Media-Alta:** Implica diseño de documento PDF y lógica de negocio estricta.
*   **Esfuerzo:** Integración de librería externa, diseño visual del PDF, endpoints de descarga binaria.
*   **Riesgo:** Diseño del PDF puede ser tedioso (alineación, fuentes).
