# Historia #12: Ver Dashboard (Cliente)

**Estado:** Ready for Dev
**Fecha Análisis Arquitectónico:** 2025-11-23
**Fecha Refinamiento:** 2025-11-23
**Origen:** Importada de HISTORIAS_DE_USUARIO.md
**Estimación:** 3 Puntos

---

**Como:** Cliente
**Quiero:** Acceder a mi portal personal (web y móvil) después de iniciar sesión.
**Para:** Ver un resumen de mis actividades.

**Criterios de Aceptación:**
*   El portal me da la bienvenida.
*   Puedo ver un resumen de mis alquileres activos y facturas pendientes.
*   Tengo acceso a las secciones "Mis Alquileres", "Mis Facturas" y "Solicitar Nuevo Alquiler".

---

## Análisis Arquitectónico (Arquitecto)

### Decisiones de Diseño

**Patrón Arquitectónico:** CQRS (Query Side) - Agregación de Datos con Seguridad

**Justificación:**
*   **Seguridad:** Filtrado estricto de datos por `clienteId` para garantizar privacidad.
*   **Eficiencia:** Consultas `COUNT` optimizadas en BD en lugar de cargar listas completas.
*   **Consistencia:** Sigue el mismo patrón de agregación que el Dashboard de Operario (HU 11).

**Componentes Principales:**

*   **Aplicación (Consulta):**
    *   `ConsultaDashboardCliente`: DTO con `clienteId`.
    *   `ManejadorDashboardCliente`: Orquestador que invoca los DAOs filtrados.
    *   `DashboardClienteDTO`: Objeto de transferencia con los KPIs.

*   **Dominio (Puertos):**
    *   `AlquilerDao`: Añadir `long contarPorClienteYEstado(Long clienteId, EstadoAlquiler)`.
    *   `FacturaDao`: Añadir `long contarPorClienteYEstado(Long clienteId, EstadoPago)`.

*   **Infraestructura:**
    *   `ConsultaControladorDashboard`: Añadir endpoint para cliente.
    *   Repositorios JPA implementando los conteos filtrados (`countByClienteIdAndEstado`).

### Especificaciones Técnicas

**API Endpoint:**
*   `GET /api/maquimu/v1/dashboard/cliente`
*   **Respuesta:**
    ```json
    {
      "alquileresActivos": 2,
      "facturasPendientes": 1
    }
    ```

**Integraciones:**
*   El manejador agrega datos de 2 fuentes distintas (Alquileres y Facturas).
*   Requiere extracción del `clienteId` del token JWT.

### Validación Arquitectónica
**Validado por:** Usuario (Luis)
**Fecha de validación:** 2025-11-23
**Feedback incorporado:**
*   ✅ Aprobado patrón de agregación con filtrado por cliente.
*   ✅ Reutilización de `ConsultaControladorDashboard`.

---

## Contexto Técnico (SM)

### Base del Refinamiento
**Análisis arquitectónico utilizado:** SÍ
**Decisiones adoptadas:** CQRS Agregación, Filtrado por Cliente.

### Análisis de Impacto
*   **Backend:**
    *   Modificar DAOs de Alquiler y Factura para soportar conteo por cliente.
    *   Extender `ConsultaControladorDashboard`.
*   **Frontend:**
    *   Nuevo componente en `ClientModule`.
    *   Integración con API.
*   **Móvil:**
    *   Nueva Activity de Dashboard.

### Componentes Técnicos a Crear/Modificar

**1. Módulo Dominio (Modificaciones):**
*   `AlquilerDao`: Añadir `long contarPorClienteYEstado(Long clienteId, EstadoAlquiler)`.
*   `FacturaDao`: Añadir `long contarPorClienteYEstado(Long clienteId, EstadoPago)`.

**2. Módulo Aplicación (Nuevo):**
*   `ConsultaDashboardCliente` (DTO con `clienteId`).
*   `ManejadorDashboardCliente`:
    *   Inyecta DAOs.
    *   Invoca conteos filtrados.
    *   Construye `DashboardClienteDTO`.

**3. Módulo Infraestructura:**
*   **Repositorios JPA:** Implementar `countByClienteIdAndEstado`.
*   **Controlador:** `ConsultaControladorDashboard` -> `GET /api/maquimu/v1/dashboard/cliente`.

**4. Frontend:**
*   `ClientDashboardComponent` (Web).
*   `ClientDashboardActivity` (Android).

---

## Tareas de Implementación (SM)

### Fase 1: Backend - Persistencia (Conteos Filtrados)
- [ ] **Actualizar Puertos y Adaptadores** (AC: 1)
    - [ ] `AlquilerDao` y `JpaAlquilerRepository`: Añadir conteo por cliente y estado.
    - [ ] `FacturaDao` y `JpaFacturaRepository`: Añadir conteo por cliente y estado.

### Fase 2: Backend - Capa de Aplicación
- [ ] **Implementar Consulta Dashboard** (AC: 1)
    - [ ] Crear `DashboardClienteDTO` (campos: alquileresActivos, facturasPendientes).
    - [ ] Crear `ManejadorDashboardCliente`:
        - [ ] Invocar `alquilerDao.contarPorClienteYEstado(clienteId, ACTIVO)`.
        - [ ] Invocar `facturaDao.contarPorClienteYEstado(clienteId, PENDIENTE)`.
        - [ ] Retornar DTO.

### Fase 3: Backend - API REST
- [ ] **Endpoint Dashboard Cliente** (AC: 2)
    - [ ] En `ConsultaControladorDashboard`:
        - [ ] Endpoint `GET /api/maquimu/v1/dashboard/cliente`.
        - [ ] Extraer `clienteId` del token JWT.
        - [ ] Invocar manejador.

### Fase 4: Frontend Web (Angular)
- [ ] **Servicio y Vista** (AC: 2, 3)
    - [ ] Actualizar `DashboardService` con `getClienteStats()`.
    - [ ] Crear `ClientDashboardComponent`.
    - [ ] Tarjetas de resumen + Accesos directos a "Mis Alquileres" y "Mis Facturas".

### Fase 5: Móvil Android
- [ ] **Dashboard Nativo** (AC: 2, 3)
    - [ ] Crear `ClientDashboardActivity`.
    - [ ] Cards con contadores.
    - [ ] Botones de navegación a otras actividades.

### Fase 6: Testing
- [ ] **Test de Seguridad**
    - [ ] Verificar que un cliente solo ve sus propios datos.

---

## Estimación (SM)

**Puntos de Historia:** 3

**Justificación:**
*   **Complejidad Media:** Agregación simple pero multi-plataforma.
*   **Esfuerzo:** Backend simple, pero requiere trabajo en Web y Móvil.
*   **Riesgo Bajo:** Patrón conocido.
