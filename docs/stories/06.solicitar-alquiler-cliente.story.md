# Historia #06: Solicitar Alquiler (Cliente)

**Estado:** ✅ Implementada
**Fecha Creación:** 2025-11-22
**Fecha Análisis Arquitectónico:** 2025-11-23
**Fecha Refinamiento:** 2025-11-23
**Fecha Implementación:** 2026-01-05
**Origen:** Importada de HISTORIAS_DE_USUARIO.md
**Estimación:** 5 Puntos

---

**Como:** Cliente
**Quiero:** Solicitar el alquiler de maquinaria desde el portal web o la app móvil.
**Para:** Iniciar un nuevo proyecto de forma rápida y sencilla.

**Criterios de Aceptación:**
*   Puedo seleccionar el tipo de maquinaria y las fechas deseadas.
*   El sistema recibe mi solicitud y la registra para aprobación de un agente.
*   Recibo una confirmación de mi solicitud.
*   El alquiler se crea con estado `PENDIENTE`.
*   Se calcula automáticamente el costo total basado en las fechas y tarifas.

---

## Análisis Arquitectónico (Arquitecto)

### Decisiones de Diseño

**Patrón Arquitectónico:** Arquitectura Hexagonal Multi-módulo + CQRS + Servicios de Dominio Reutilizables

**Justificación:**
*   Alineación estricta con la arquitectura definida en HU 01 (multi-módulo Gradle).
*   Aplicación de CQRS: Solicitar Alquiler (Comando), Consultar Disponibilidad (Consulta).
*   **Servicios de Dominio** para lógica de negocio reutilizable (cálculo de costos, validación de disponibilidad).
*   **Servicios de Aplicación** para orquestación de casos de uso complejos.
*   Patrón consistente con HU 04, 05, 06 + lógica de negocio adicional.

**Modelo de Datos (Entidad `Alquiler`):**
*   `alquilerId`: Long (PK, Auto-increment) - Mapea a `alquiler_id` en BD.
*   `clienteId`: Long (FK a clientes, Not Null) - Mapea a `cliente_id` en BD.
*   `maquinariaId`: Long (FK a maquinaria, Not Null) - Mapea a `maquinaria_id` en BD.
*   `usuarioId`: Long (FK a usuarios, **NULLABLE**) - Mapea a `usuario_id` en BD.
*   `fechaInicio`: LocalDateTime (Not Null) - Mapea a `fecha_inicio` en BD.
*   `fechaFin`: LocalDateTime (Not Null) - Mapea a `fecha_fin` en BD.
*   `costoTotal`: BigDecimal (Calculado automáticamente) - Mapea a `costo_total` en BD.
*   `estado`: Enum [`PENDIENTE`, `APROBADO`, `ACTIVO`, `FINALIZADO`, `CANCELADO`] - Mapea a `estado_alquiler` en BD.

**Máquina de Estados del Alquiler:**
```
PENDIENTE (solicitud cliente, usuarioId = null)
   ↓
APROBADO (agente aprueba, asigna usuarioId)
   ↓
ACTIVO (alquiler en curso)
   ↓
FINALIZADO (completado) o CANCELADO (cancelado)
```

**Cálculo de Costos:**
*   **Fase 1 (HU 07):** Cálculo automático por días completos.
    *   Fórmula: `costoEstimado = DAYS_BETWEEN(fechaInicio, fechaFin) * maquinaria.tarifaPorDia`
*   **Fase 2 (HU 09):** Operario ajusta días y horas cobradas.
    *   Fórmula: `costoFinal = (diasCobrados * tarifaDia) + (horasCobradas * tarifaHora)`

**Validación de Disponibilidad:**
*   Una maquinaria NO puede estar en dos alquileres simultáneamente.
*   Estados que bloquean disponibilidad: `APROBADO`, `ACTIVO`.
*   Estados que NO bloquean: `PENDIENTE`, `FINALIZADO`, `CANCELADO`.

**Estructura de Módulos (Backend):**

#### **1. Módulo Dominio (`dominio/`)**

*   **Modelo:**
    *   `dominio/modelo/Alquiler.java` - Entidad de dominio pura (sin JPA).
    *   `dominio/modelo/EstadoAlquiler.java` - Enum: `PENDIENTE`, `APROBADO`, `ACTIVO`, `FINALIZADO`, `CANCELADO`.

*   **Servicios de Dominio (Reutilizables):**
    *   `dominio/servicio/CalculadorCostoAlquiler.java`:
        *   `BigDecimal calcularCostoPorDias(Maquinaria, LocalDateTime fechaInicio, LocalDateTime fechaFin)`
        *   `BigDecimal calcularCostoMixto(Maquinaria, int diasCobrados, int horasCobradas)` (Fase 2)
        *   **Usado en:** HU 07, HU 08, HU 09, HU 10.
    *   `dominio/servicio/ValidadorDisponibilidadMaquinaria.java`:
        *   `boolean estaDisponible(Long maquinariaId, LocalDateTime fechaInicio, LocalDateTime fechaFin)`
        *   **Usado en:** HU 07, HU 08, HU 09.

*   **Puertos (Interfaces):**
    *   `dominio/puerto/dao/AlquilerDao.java` - Lectura:
        *   `List<Alquiler> listarTodos()`
        *   `Optional<Alquiler> buscarPorId(Long id)`
        *   `List<Alquiler> buscarPorCliente(Long clienteId)`
        *   `List<Alquiler> buscarPorMaquinariaYRangoFechas(Long maquinariaId, LocalDateTime inicio, LocalDateTime fin)`
    *   `dominio/puerto/repositorio/AlquilerRepositorio.java` - Escritura:
        *   `Alquiler guardar(Alquiler alquiler)`
        *   `void eliminar(Long id)`

#### **2. Módulo Aplicación (`aplicacion/`)**

*   **Servicios de Aplicación (Orquestadores):**
    *   `aplicacion/servicio/ServicioGestionAlquiler.java`:
        *   `Alquiler solicitarAlquiler(ComandoSolicitarAlquiler)` - Orquesta lógica de solicitud.
        *   `Alquiler aprobarAlquiler(ComandoAprobarAlquiler)` - Futuro (HU 09).
        *   `Alquiler activarAlquiler(ComandoActivarAlquiler)` - Futuro (HU 09).
        *   `Alquiler finalizarAlquiler(ComandoFinalizarAlquiler)` - Futuro (HU 09).

*   **CQRS - Comandos (Escritura):**
    *   `aplicacion/comando/ComandoSolicitarAlquiler.java` - DTO inmutable: clienteId, maquinariaId, fechaInicio, fechaFin.
    *   `aplicacion/comando/manejador/ManejadorSolicitarAlquiler.java`:
        *   Invoca `ServicioGestionAlquiler.solicitarAlquiler()`.

*   **CQRS - Consultas (Lectura):**
    *   `aplicacion/consulta/ConsultaMaquinariasDisponibles.java` - DTO: fechaInicio, fechaFin.
    *   `aplicacion/consulta/manejador/ManejadorMaquinariasDisponibles.java`:
        *   Obtener maquinarias disponibles en rango de fechas.
        *   Usa `ValidadorDisponibilidadMaquinaria` para filtrar.

#### **3. Módulo Infraestructura (`infraestructura/`)**

*   **Adaptadores de Persistencia:**
    *   `infraestructura/adaptador/entidad/AlquilerEntity.java` - Entidad JPA.
    *   `infraestructura/adaptador/dao/JpaAlquilerDao.java` - Implementa `AlquilerDao`.
    *   `infraestructura/adaptador/repositorio/JpaAlquilerRepositorio.java` - Implementa `AlquilerRepositorio`.
    *   `infraestructura/adaptador/repositorio/JpaAlquilerRepository.java` - Spring Data JPA interface.

*   **Controladores REST (Separados por CQRS):**
    *   `infraestructura/controlador/ComandoControladorAlquiler.java`:
        *   `POST /api/maquimu/v1/alquileres` → `ManejadorSolicitarAlquiler`
    *   `infraestructura/controlador/ConsultaControladorAlquiler.java`:
        *   `GET /api/maquimu/v1/maquinarias/disponibles?fechaInicio=X&fechaFin=Y` → `ManejadorMaquinariasDisponibles`

### Especificaciones Técnicas

**API Endpoints:**
*   `POST /api/maquimu/v1/alquileres` - Solicitar nuevo alquiler (estado: PENDIENTE).
    *   Body: `{clienteId, maquinariaId, fechaInicio, fechaFin}`
    *   Response: `{alquilerId, clienteId, maquinariaId, fechaInicio, fechaFin, costoTotal, estado: "PENDIENTE"}`
*   `GET /api/maquimu/v1/maquinarias/disponibles?fechaInicio=X&fechaFin=Y` - Listar maquinarias disponibles.
    *   Response: `[{maquinariaId, nombreEquipo, marca, modelo, tarifaPorDia, tarifaPorHora}, ...]`

**Base de Datos:**
*   Tabla `alquileres` (ya existe).
*   **Migración requerida:** `V3__update_alquileres_table.sql`:
    *   Actualizar enum `estado_alquiler`: añadir `pendiente`, `aprobado`.
    *   Permitir `usuario_id` NULL.

**Validaciones:**
*   Fecha fin > Fecha inicio.
*   Maquinaria disponible en el rango de fechas (no en APROBADO/ACTIVO).
*   Cliente autenticado (JWT).
*   Cliente y maquinaria existen en BD.

**Frontend (Angular):**
*   **Módulo:** `ClientModule`.
*   **Componentes:**
    *   `RequestRentalComponent`: Formulario de solicitud.
    *   Selector de maquinaria (dropdown con disponibles).
    *   Date pickers para fechas.
    *   Mostrar costo estimado en tiempo real.
*   **Servicios:** `AlquilerService` (consume endpoints `/alquileres`).

**Móvil (Android):**
*   **Activities:** `RequestRentalActivity`.
*   **Repository:** `AlquilerRepository`.

### Impacto Arquitectónico

**Componentes Nuevos:**
*   Todos los componentes de Alquiler en los 3 módulos.
*   **2 Servicios de Dominio reutilizables** (CalculadorCosto, ValidadorDisponibilidad).
*   **1 Servicio de Aplicación orquestador** (ServicioGestionAlquiler).

**Componentes Modificados:**
*   Ninguno (no modifica componentes existentes).

**Documentación a Actualizar:**
*   `docs/architecture/source-tree.md` - Añadir estructura de paquetes de alquileres.
*   Crear `docs/architecture/flujo-solicitar-alquiler.md` - Documentar flujo de negocio.

**Riesgos Arquitectónicos:**
1. **Migración de BD requerida:** Actualizar enum y permitir NULL en `usuario_id`.
2. **Lógica de disponibilidad compleja:** Consultas con rangos de fechas pueden ser lentas.
3. **Cálculo de costos:** Fase 1 simple (días), Fase 2 requiere ajuste manual por operario.

### Validación Arquitectónica

**Validado por:** Usuario (Luis)
**Fecha de validación:** 2025-11-23
**Feedback incorporado:**
*   ✅ Aprobado migración BD: añadir estados `pendiente` y `aprobado`.
*   ✅ Aprobado `usuario_id` NULL para estado PENDIENTE.
*   ✅ Aprobado cálculo de costos: Fase 1 por días, Fase 2 mixto (días + horas).
*   ✅ Aprobado disponibilidad: bloquear en APROBADO y ACTIVO.
*   ✅ Aprobado arquitectura de servicios: Manejador → Servicio Aplicación → Servicios Dominio.

### Referencias Arquitectónicas

**Documentación consultada:**
*   `docs/architecture/ARQUITECTURA.md` - Patrón Hexagonal + CQRS.
*   `docs/architecture/tech-stack.md` - Spring Boot, MySQL, JPA.
*   `docs/architecture/source-tree.md` - Estructura de módulos.
*   `docs/architecture/BASE_DATOS.md` - Tabla `alquileres`.

**Historias relacionadas:**
*   HU 01 - Configuración Inicial Backend (estructura multi-módulo).
*   HU 04 - Registro de Maquinaria (entidad relacionada).
*   HU 06 - Registro de Clientes (entidad relacionada).
*   HU 05 - Autenticación (cliente autenticado).

**Patrones aplicados:**
*   Hexagonal Architecture (Puertos y Adaptadores).
*   CQRS (Command Query Responsibility Segregation).
*   Domain Services (Servicios de Dominio Reutilizables).
*   Application Services (Orquestadores de Casos de Uso).
*   State Machine (Máquina de Estados del Alquiler).

---

## Contexto Técnico (SM)

### Base del Refinamiento

**Análisis arquitectónico utilizado:** SÍ

**Decisiones arquitectónicas adoptadas:**
*   Arquitectura Hexagonal Multi-módulo con separación física en 3 módulos Gradle.
*   Implementación de CQRS: Solicitar Alquiler (Comando), Consultar Disponibilidad (Consulta).
*   **Servicios de Dominio reutilizables:** `CalculadorCostoAlquiler`, `ValidadorDisponibilidadMaquinaria`.
*   **Servicio de Aplicación orquestador:** `ServicioGestionAlquiler`.
*   Máquina de estados: PENDIENTE → APROBADO → ACTIVO → FINALIZADO/CANCELADO.
*   Migración de BD requerida.

**Componentes validados:**
*   **Módulo `dominio`**: Alquiler, EstadoAlquiler, 2 Servicios de Dominio, 2 Puertos.
*   **Módulo `aplicacion`**: 1 Servicio Orquestador, 1 Comando, 1 Consulta, 2 Manejadores.
*   **Módulo `infraestructura`**: Entidades JPA, Adaptadores, 2 Controladores.

**Patrones seguidos:**
*   Puertos y Adaptadores (Hexagonal Architecture).
*   CQRS para separación de lectura/escritura.
*   Domain Services (Servicios de Dominio Reutilizables).
*   Application Services (Orquestadores de Casos de Uso).
*   State Machine (Máquina de Estados).

### Análisis de Impacto

*   **Base de Datos:** 
    *   Tabla `alquileres` ya existe.
    *   **Migración requerida:** `V3__update_alquileres_table.sql`.
    *   Actualizar enum `estado_alquiler`: añadir `pendiente`, `aprobado`.
    *   Permitir `usuario_id` NULL.
*   **Estructura de Archivos:** 
    *   Crear componentes en los 3 módulos (dominio, aplicacion, infraestructura).
    *   **Nuevos:** Servicios de Dominio, Servicio de Aplicación.
*   **Integración:** 
    *   Requiere acceso a `ClienteDao`, `MaquinariaDao` (de HU 04, 06).
    *   Consultas complejas con rangos de fechas.
*   **Frontend:** 
    *   Requiere crear componentes en `ClientModule`.
    *   Selector de maquinaria con disponibilidad en tiempo real.
    *   Cálculo de costo estimado dinámico.

### Componentes Técnicos a Crear

**1. Módulo Dominio (`dominio/`):**

*   **Modelos:**
    *   `dominio/modelo/Alquiler.java` - Entidad de dominio pura.
    *   `dominio/modelo/EstadoAlquiler.java` - Enum: `PENDIENTE`, `APROBADO`, `ACTIVO`, `FINALIZADO`, `CANCELADO`.

*   **Servicios de Dominio (Reutilizables):**
    *   `dominio/servicio/CalculadorCostoAlquiler.java` - Cálculo de costos.
    *   `dominio/servicio/ValidadorDisponibilidadMaquinaria.java` - Validación de disponibilidad.

*   **Puertos:**
    *   `dominio/puerto/dao/AlquilerDao.java` - Interface de lectura.
    *   `dominio/puerto/repositorio/AlquilerRepositorio.java` - Interface de escritura.

**2. Módulo Aplicación (`aplicacion/`):**

*   **Servicios de Aplicación:**
    *   `aplicacion/servicio/ServicioGestionAlquiler.java` - Orquestador de lógica de alquileres.

*   **CQRS - Comandos:**
    *   `aplicacion/comando/ComandoSolicitarAlquiler.java` - DTO inmutable.
    *   `aplicacion/comando/manejador/ManejadorSolicitarAlquiler.java` - Handler que delega en servicio.

*   **CQRS - Consultas:**
    *   `aplicacion/consulta/ConsultaMaquinariasDisponibles.java` - DTO inmutable.
    *   `aplicacion/consulta/manejador/ManejadorMaquinariasDisponibles.java` - Handler de disponibilidad.

**3. Módulo Infraestructura (`infraestructura/`):**

*   **Adaptadores de Persistencia:**
    *   `infraestructura/adaptador/entidad/AlquilerEntity.java` - Entidad JPA.
    *   `infraestructura/adaptador/dao/JpaAlquilerDao.java` - Implementación de AlquilerDao.
    *   `infraestructura/adaptador/repositorio/JpaAlquilerRepositorio.java` - Implementación de AlquilerRepositorio.
    *   `infraestructura/adaptador/repositorio/JpaAlquilerRepository.java` - Spring Data JPA interface.

*   **Controladores REST:**
    *   `infraestructura/controlador/ComandoControladorAlquiler.java` - Endpoint de solicitud.
    *   `infraestructura/controlador/ConsultaControladorAlquiler.java` - Endpoint de disponibilidad.

**4. Frontend (Angular):**

*   `RequestRentalComponent`, `AlquilerService`.

**5. Móvil (Android):**

*   `RequestRentalActivity`, `AlquilerRepository`.

### Verificación de Completitud

**Criterios de verificación:**
- [x] Los 3 módulos compilan sin errores (`./gradlew build`).
- [x] La migración Flyway `V13__update_alquileres_for_solicitudes.sql` se ejecuta correctamente.
- [x] Los endpoints `/alquileres` y `/maquinarias/disponibles` responden correctamente.
- [x] El cálculo de costos funciona correctamente.
- [x] La validación de disponibilidad funciona (bloquea APROBADO y ACTIVO).
- [x] El estado inicial es PENDIENTE con `usuarioId` NULL.
- [x] El frontend puede solicitar alquileres y ver maquinarias disponibles.

---

## Implementación Completada (2026-01-05)

### Archivos Creados

#### Backend - Dominio (`dominio/`)

| Archivo | Descripción |
|---------|-------------|
| `alquiler/modelo/EstadoAlquiler.java` | Enum: PENDIENTE, APROBADO, ACTIVO, FINALIZADO, CANCELADO |
| `alquiler/modelo/Alquiler.java` | Entidad de dominio con máquina de estados |
| `alquiler/servicio/CalculadorCostoAlquiler.java` | Cálculo de costos por días y mixto |
| `alquiler/servicio/ValidadorDisponibilidadMaquinaria.java` | Validación de disponibilidad |
| `alquiler/puerto/dao/AlquilerDao.java` | Puerto de lectura |
| `alquiler/puerto/repositorio/AlquilerRepositorio.java` | Puerto de escritura |

#### Backend - Aplicación (`aplicacion/`)

| Archivo | Descripción |
|---------|-------------|
| `alquiler/comando/ComandoSolicitarAlquiler.java` | DTO inmutable del comando |
| `alquiler/comando/manejador/ManejadorSolicitarAlquiler.java` | Handler con @Service y @Transactional |
| `alquiler/consulta/ConsultaMaquinariasDisponibles.java` | Consulta de maquinarias disponibles |
| `alquiler/consulta/ConsultaAlquileresCliente.java` | Consulta de alquileres por cliente |
| `alquiler/consulta/manejador/ManejadorMaquinariasDisponibles.java` | Handler de disponibilidad |
| `alquiler/consulta/manejador/ManejadorAlquileresCliente.java` | Handler de alquileres cliente |
| `alquiler/servicio/ServicioGestionAlquiler.java` | Orquestador con validaciones |

#### Backend - Infraestructura (`infraestructura/`)

| Archivo | Descripción |
|---------|-------------|
| `db/migration/ddl/V13__update_alquileres_for_solicitudes.sql` | Migración BD |
| `adaptador/entidad/AlquilerEntity.java` | Entidad JPA con relaciones |
| `adaptador/repositorio/JpaAlquilerRepository.java` | Spring Data con queries |
| `adaptador/alquiler/AlquilerRepositorioJpa.java` | Implementación puerto escritura |
| `adaptador/alquiler/AlquilerDaoJpa.java` | Implementación puerto lectura |
| `configuracion/AlquilerConfig.java` | @Configuration para beans de dominio |
| `controlador/ComandoControladorAlquiler.java` | POST /api/maquimu/v1/alquileres |
| `controlador/ConsultaControladorAlquiler.java` | GET endpoints de consulta |

#### Frontend (`maquimu-frontend/`)

| Archivo | Descripción |
|---------|-------------|
| `core/models/alquiler.model.ts` | Interfaces TypeScript |
| `core/services/alquiler.service.ts` | Servicio HTTP |
| `client/request-rental/request-rental.component.ts` | Componente de solicitud |
| `client/request-rental/request-rental.component.html` | Template con pasos |
| `client/request-rental/request-rental.component.css` | Estilos |
| `client/client-routing.module.ts` | Ruta `/client/request-rental` |

### Endpoints API Implementados

```
POST   /api/maquimu/v1/alquileres                     # Solicitar alquiler
GET    /api/maquimu/v1/maquinarias/disponibles        # Listar disponibles (fechaInicio, fechaFin)
GET    /api/maquimu/v1/clientes/{clienteId}/alquileres # Alquileres de cliente
```

### Cambios Adicionales

- Agregado `getId()` en `Maquinaria.java` y `Alquiler.java` del dominio
- Configuración de beans de servicios de dominio en `AlquilerConfig.java` (sin @Component en dominio)

---

## Tareas de Implementación (SM) - COMPLETADAS

### Fase 0: Migración de Base de Datos

- [x] **Crear migración Flyway** (AC: 4)
    - [x] Creado `V13__update_alquileres_for_solicitudes.sql` en `infraestructura/src/main/resources/db/migration/ddl/`
    - [x] Actualizado enum `estado_alquiler` a ENUM con valores requeridos
    - [x] Permitido `usuario_id` NULL
    - [x] Convertido IDs a BIGINT para consistencia con JPA

### Fase 1: Módulo Dominio (Java Puro)

- [x] **Crear Modelos de Dominio** (AC: 1, 4)
    - [x] Creado `dominio/alquiler/modelo/Alquiler.java` (sin anotaciones JPA)
    - [x] Creado `dominio/alquiler/modelo/EstadoAlquiler.java`
    - [x] Métodos de transición de estado: aprobar(), activar(), finalizar(), cancelar()

- [x] **Crear Servicios de Dominio** (AC: 2, 5)
    - [x] Creado `dominio/alquiler/servicio/CalculadorCostoAlquiler.java`
    - [x] Creado `dominio/alquiler/servicio/ValidadorDisponibilidadMaquinaria.java`

- [x] **Crear Puertos (Interfaces)** (AC: 1, 2, 3)
    - [x] Creado `dominio/alquiler/puerto/dao/AlquilerDao.java`
    - [x] Creado `dominio/alquiler/puerto/repositorio/AlquilerRepositorio.java`

### Fase 2: Módulo Aplicación (CQRS + Servicios)

- [x] **Crear Servicio de Aplicación Orquestador** (AC: 1, 2, 5)
    - [x] Creado `aplicacion/alquiler/servicio/ServicioGestionAlquiler.java`

- [x] **Implementar Comando de Solicitud** (AC: 1, 3)
    - [x] Creado `aplicacion/alquiler/comando/ComandoSolicitarAlquiler.java`
    - [x] Creado `aplicacion/alquiler/comando/manejador/ManejadorSolicitarAlquiler.java`

- [x] **Implementar Consultas** (AC: 1)
    - [x] Creado `aplicacion/alquiler/consulta/ConsultaMaquinariasDisponibles.java`
    - [x] Creado `aplicacion/alquiler/consulta/ConsultaAlquileresCliente.java`
    - [x] Creado `aplicacion/alquiler/consulta/manejador/ManejadorMaquinariasDisponibles.java`
    - [x] Creado `aplicacion/alquiler/consulta/manejador/ManejadorAlquileresCliente.java`

### Fase 3: Módulo Infraestructura (Spring Boot)

- [x] **Crear Entidad JPA** (AC: 1, 4)
    - [x] Creado `infraestructura/adaptador/entidad/AlquilerEntity.java`

- [x] **Implementar Adaptadores de Persistencia** (AC: 1, 2, 3)
    - [x] Creado `infraestructura/adaptador/repositorio/JpaAlquilerRepository.java`
    - [x] Creado `infraestructura/adaptador/alquiler/AlquilerDaoJpa.java`
    - [x] Creado `infraestructura/adaptador/alquiler/AlquilerRepositorioJpa.java`

- [x] **Implementar Controladores** (AC: 1, 3)
    - [x] Creado `infraestructura/controlador/ComandoControladorAlquiler.java`
    - [x] Creado `infraestructura/controlador/ConsultaControladorAlquiler.java`

- [x] **Configuración de Beans**
    - [x] Creado `infraestructura/configuracion/AlquilerConfig.java`

### Fase 4: Frontend Web (Angular)

- [x] **Crear Servicio Frontend** (AC: 1, 3)
    - [x] Creado `AlquilerService` en `core/services/alquiler.service.ts`

- [x] **Implementar Componente de Solicitud** (AC: 1, 2, 3, 5)
    - [x] Creado `RequestRentalComponent` en `client/request-rental/`
    - [x] Formulario con selector de fechas y maquinaria
    - [x] Cálculo de costo estimado en tiempo real
    - [x] Proceso de 3 pasos: Fechas → Selección → Confirmación

### Fase 5: Móvil (Android)

- [ ] **Pendiente para HU #13** - Configuración Inicial Móvil
        - [ ] `getMaquinariasDisponibles(fechaInicio: Date, fechaFin: Date): Observable<Maquinaria[]>`
        - [ ] `solicitarAlquiler(data: SolicitarAlquilerRequest): Observable<Alquiler>`
        - [ ] `calcularCostoEstimado(maquinariaId: number, fechaInicio: Date, fechaFin: Date): Observable<number>`

- [ ] **Implementar Componente de Solicitud** (AC: 1, 2, 3, 5)
    - [ ] Crear `RequestRentalComponent` en `client/rental`
    - [ ] Formulario reactivo con campos:
        - [ ] Selector de maquinaria (dropdown dinámico con disponibles)
        - [ ] Date picker para fecha inicio
        - [ ] Date picker para fecha fin
        - [ ] Mostrar costo estimado en tiempo real
    - [ ] Lógica:
        - [ ] Al cambiar fechas: Recargar maquinarias disponibles
        - [ ] Al seleccionar maquinaria: Calcular y mostrar costo estimado
        - [ ] Al enviar: Llamar `AlquilerService.solicitarAlquiler()`
        - [ ] Mostrar confirmación con número de alquiler y estado PENDIENTE

### Fase 5: Móvil (Android)

- [ ] **Crear Modelos de Datos** (AC: 1)
    - [ ] Crear data classes: `Alquiler`, `SolicitarAlquilerRequest`, `EstadoAlquiler`

- [ ] **Implementar AlquilerRepository** (AC: 1, 3)
    - [ ] Crear `AlquilerRepository` en `data/repository`
    - [ ] Métodos:
        - [ ] `getMaquinariasDisponibles(fechaInicio: Date, fechaFin: Date): Flow<List<Maquinaria>>`
        - [ ] `solicitarAlquiler(request: SolicitarAlquilerRequest): Flow<Alquiler>`

- [ ] **Crear RequestRentalActivity** (AC: 1, 2, 3)
    - [ ] Crear layout `activity_request_rental.xml`
    - [ ] Spinner para maquinaria (carga disponibles según fechas)
    - [ ] Date pickers para fechas
    - [ ] TextView para mostrar costo estimado
    - [ ] ViewModel: `RequestRentalViewModel`
    - [ ] Lógica: Similar a frontend web

### Fase 6: Verificación y Testing

- [ ] **Verificar compilación y arranque**
    - [ ] Ejecutar `./gradlew build` sin errores
    - [ ] Ejecutar `./gradlew bootRun` y verificar arranque exitoso
    - [ ] Verificar que la migración Flyway se ejecuta correctamente
    - [ ] Verificar que los endpoints `/alquileres` y `/maquinarias/disponibles` están accesibles

- [ ] **Testing de Backend**
    - [ ] Test unitario: `CalculadorCostoAlquiler` calcula correctamente
    - [ ] Test unitario: `ValidadorDisponibilidadMaquinaria` valida correctamente
    - [ ] Test unitario: `ServicioGestionAlquiler.solicitarAlquiler()` orquesta correctamente
    - [ ] Test de integración: Solicitar alquiler completo
    - [ ] Test de integración: Validación de disponibilidad (bloquea APROBADO/ACTIVO)
    - [ ] Test de integración: Consultar maquinarias disponibles

- [ ] **Testing de Frontend**
    - [ ] Test unitario: `AlquilerService` llama a endpoints correctamente
    - [ ] Test unitario: `RequestRentalComponent` calcula costo estimado
    - [ ] Test de integración: Flujo completo de solicitud de alquiler

### Fase 7: Documentación del Proyecto

- [ ] **Documentar arquitectura implementada**
    - [ ] Sugerir al usuario usar `*ARCHITECT` → `*documentar-componente` para documentar el módulo de alquileres
    - [ ] Sugerir al usuario usar `*ARCHITECT` → `*documentar-flujo-negocio` para documentar el flujo de solicitud de alquiler
    - [ ] Actualizar `source-tree.md` con la estructura de paquetes de alquileres

---

## Notas de Implementación

**Dependencias Gradle clave:**
*   `spring-boot-starter-web`, `spring-boot-starter-data-jpa`
*   `mysql-connector-j`, `flyway-core`, `flyway-mysql`
*   `lombok` (opcional, para reducir boilerplate)

**Mapeo de Nombres:**
*   **Java (camelCase):** `clienteId`, `maquinariaId`, `usuarioId`, `fechaInicio`, `fechaFin`, `costoTotal`
*   **BD (snake_case):** `cliente_id`, `maquinaria_id`, `usuario_id`, `fecha_inicio`, `fecha_fin`, `costo_total`
*   **Enum Estado:**
    *   Java: `PENDIENTE`, `APROBADO`, `ACTIVO`, `FINALIZADO`, `CANCELADO`
    *   BD: `pendiente`, `aprobado`, `activo`, `finalizado`, `cancelado`

**Consulta de Disponibilidad (SQL):**
```sql
SELECT * FROM maquinaria m
WHERE m.maquinaria_id NOT IN (
  SELECT a.maquinaria_id FROM alquileres a
  WHERE a.estado_alquiler IN ('aprobado', 'activo')
  AND (
    (a.fecha_inicio <= :fechaFin AND a.fecha_fin >= :fechaInicio)
  )
)
```

**Próximos pasos después de completar esta historia:**
1.  Implementar la funcionalidad usando las tareas definidas.
2.  Actualizar documentación arquitectónica con `*ARCHITECT`.
3.  Los servicios de dominio (`CalculadorCostoAlquiler`, `ValidadorDisponibilidadMaquinaria`) serán reutilizados en HU 08, 09, 10.

---

## Estimación (SM)

### Análisis de Complejidad

- **Nivel de complejidad**: **Alta**
- **Justificación**: 
  - Lógica de negocio compleja (cálculo de costos, validación de disponibilidad)
  - Máquina de estados (PENDIENTE → APROBADO → ACTIVO → FINALIZADO/CANCELADO)
  - Servicios de Dominio reutilizables (nueva capa arquitectónica)
  - Servicios de Aplicación orquestadores (nueva capa arquitectónica)
  - Integración entre 3 entidades (Cliente, Maquinaria, Usuario)
  - Consultas complejas con rangos de fechas
  - Migración de BD requerida
  - Full-Stack (Backend + Frontend + Móvil)
  
- **Refactorización requerida**: **Sí** - Migración de BD
- **Precedentes encontrados**: **Parcial** - Patrón CQRS establecido, pero servicios de dominio son nuevos

### Análisis de Riesgo

- **Porcentaje de desviación estimado**: 25-30%
- **Factores de riesgo**:
  - Primera implementación de Servicios de Dominio reutilizables
  - Primera implementación de Servicios de Aplicación orquestadores
  - Lógica de disponibilidad compleja (consultas con rangos de fechas)
  - Migración de BD puede afectar datos existentes
  - Cálculo de costos debe ser preciso (crítico para negocio)
  - Máquina de estados requiere validaciones estrictas
  
- **Recomendaciones**:
  - Validar migración de BD en entorno de desarrollo antes de producción
  - Probar exhaustivamente la lógica de disponibilidad con casos edge
  - Implementar tests unitarios para servicios de dominio
  - Documentar claramente la máquina de estados
  - Considerar índices en BD para consultas de disponibilidad (performance)

---

**Estimación completada por:** SM (Scrum Master)  
**Fecha de estimación:** 2025-11-23  
**Método utilizado:** Análisis por tareas con lógica de negocio compleja y nuevas capas arquitectónicas
